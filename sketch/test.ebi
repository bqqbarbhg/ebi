
struct Vec2 {
    a: Int
    b: Int
}

def dot(a: Vec2, b: Vec2): Int {
    return a.x*b.x + a.y*b.y
}

def main() {
    let a: Vec2 = { 1, 2 }
    let b: Vec2 = { 3, 4 }
    print("dot product:", dot(a, b))
}
